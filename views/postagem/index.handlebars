<div class="card">
    <div class="card-body">
        <h1>{{postagem.titulo}}</h1>
        <hr>
        <small'mt-4'>Data de publicação: {{postagem.data}}</small><br>
        <hr>
       <p'mt-4'>{{postagem.conteudo}}</p>
    </div>
</div>